# PostgreSQL connection string used by the backend service.
DATABASE_URL=postgresql://scryer:scryer@postgres:5432/scryer

# Port exposed by the FastAPI service on your local machine.
API_PORT=8000

# Local development secret used for signing or encrypting sensitive values.
SECRET_KEY=replace-me-with-a-local-secret
